<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="{{path('home')}}">Symfony App</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <a class="nav-link" href="{{ path('custom', {name: 'beau carnes'})}}">Greet beau carnes</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('post.index')}}">Posts</a>
      </li> 
      <li class="nav-item">
        <a class="nav-link" href="{{ path('post.create')}}">New Post</a>
      </li> 
      {% if is_granted('IS_AUTHENTICATED_ANONYMOUSLY') %}
      <li class="nav-item float-right">
        <a class="nav-link" href="{{ path('app_login')}}">Login</a>
      </li> 
      {% endif %}
      
      <li class="nav-item float-right">
        <a class="nav-link" href="{{ path('app_logout')}}">Logout</a>
      </li> 
      
    </ul>
  </div>
</nav>